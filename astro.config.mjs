// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    base: '/mi-portafolio-Lab-05-07/', //!mismo nombre que el repo de github
    output: 'static', //!para que el sitio sea esttico
});
